# Code Jam template

```cpp
#include <bits/stdc++.h>
using namespace std;
void resolve(int k){
  // TODO
  cout << "Case #" << k << ": " << ans << "\n";
}
int main(){
  ios::sync_with_stdio(0);
  cin.tie(0);
  //cout.precision(10);
  //cout << fixed;
  int T;
  cin >> T;
  for(int k = 1; k <= T; ++k) resolve(k);
  return 0;
}
```

