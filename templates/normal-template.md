# Normal template

```cpp
#pragma GCC optimize("O3")
#include <bits/stdc++.h>
using namespace std;
using ll = int64_t;
const ll inf = (LLONG_MAX>>2);
int main(){
   ios::sync_with_stdio(false);
   cin.tie(nullptr);
   cout << fixed;
   cout.precision(10);
   // TODO
   return 0;
}
```



